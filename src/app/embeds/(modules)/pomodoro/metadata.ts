import type { IModuleMetadata } from '~/lib/embeds/modules';

const metadata: IModuleMetadata = {
  name: 'Pomodoro',
  description: 'Pomodoro timer which can be embedded in any application',
  tags: [],
};

export default metadata;
