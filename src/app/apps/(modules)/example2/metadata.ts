import type { IModuleMetadata } from '~/lib/modules';

const metadata: IModuleMetadata = {
  name: 'Example 2',
  description: 'An outline for the main function and purpose of this app.',
  tags: ['miscellaneous'],
};

export default metadata;
